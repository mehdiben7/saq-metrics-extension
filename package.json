{
  "name": "saq-metrics-extension",
  "version": "1.0.0",
  "description": "An extension that computes prices/l and prices/l of alcool",
  "scripts": {
    "build": "vite build && cp manifest.json dist/",
    "package": "rm -f saq-metrics-extension.zip && npm run build && cd dist && zip -r ../saq-metrics-extension.zip ."
  },
  "author": "Mehdi Benouhoud",
  "license": "LiLiQ-P-1.1",
  "devDependencies": {
    "@types/chrome": "^0.0.326",
    "@types/firefox-webext-browser": "^120.0.4",
    "typescript": "^5.8.3",
    "vite": "^6.3.5"
  },
  "type": "module"
}
